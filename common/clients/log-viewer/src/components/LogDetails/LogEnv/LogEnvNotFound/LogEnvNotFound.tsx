import { Typography } from '@material-ui/core';

export const LogEnvNotFound = () => {
  return (
    <div>
      <Typography variant="h5">Environment</Typography>

      <Typography>
        No environment found.
      </Typography>
    </div>
  );
}
