//import { transactionContext } from './TransactionContext';
export * from './TransactionContext';
/*
const myFunc = async () => {
  console.log('my func')
}

(async () => {
  const trans = {
    label: '',
    topics: [],
    context: {},
  };

  await transactionContext.run(trans, myFunc);
})()
*/
