import { makeStyles } from '../../../makeStyles';

export const classes = makeStyles({
  root: {
    top: 'center',
    left: 'center',
    width: '50%',
    height: '50%',
    border: {
      type: 'line',
      fg: 'blue',
    },
  },
});
