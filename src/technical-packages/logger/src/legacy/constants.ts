import path from 'path';

export const PROJECT_DIR = path.join(__dirname, '..');
export const PROJECT_ROOT_DIR = path.join(PROJECT_DIR, '../../..');
export const PROJECT_ROOT_SRC_DIR = path.join(PROJECT_ROOT_DIR, 'src');
export const SOURCE_DIR = path.join(PROJECT_DIR, 'src');
